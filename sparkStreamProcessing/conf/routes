# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

GET     /twitter/transformation               controllers.Application.twitterTransformation()

GET     /twitter/transformation/statistics    controllers.Application.twitterTransformationStatistics()

GET     /callback                   org.pac4j.play.CallbackController.callback()
POST    /callback                   org.pac4j.play.CallbackController.callback()
GET     /logout                     org.pac4j.play.CallbackController.logoutAndRedirect()

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file               controllers.Assets.at(path="/public", file)
